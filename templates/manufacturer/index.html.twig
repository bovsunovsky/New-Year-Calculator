{% extends 'base.html.twig' %}
{% block styles %}
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
{% endblock %}

{% block body %}
    <h2> Все производители </h2>
    <a class="btn btn-primary" href="{{ path('app_create_manufacturer') }}">Добавить производителя</a>

    <table class="table">
        <thead>
        <tr>
            <th>ID</th>
            <th>Название фирмы</th>

        </tr>
        </thead>
        <tbody>
        {% for manufacturer in manufacturerList %}
            <tr>
                <td>{{ manufacturer.id }}</td>
                <td>{{ manufacturer.name }}</td>

                <td>
                    <a href="{{ path('app_update_manufacturer', {'id': manufacturer.id}) }}"><img src="/img/edit.png"></a>
                    <a href="{{path('app_delete_manufacturer', {'id': manufacturer.id})}}"><img src="/img/delete.png"></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}